// This workflow was automatically generated by the operandi_utils.oton module
nextflow.enable.dsl = 2

params.input_file_group = "OCR-D-GT-SEG-BLOCK,OCR-D-OCR"
params.mets_path = "null"
params.workspace_dir = "null"
params.pages = "null"
params.forks = "4"
params.env_wrapper_cmd_step0 = "null"
params.env_wrapper_cmd_step1 = "null"
params.env_wrapper_cmd_step2 = "null"

log.info """\
    OPERANDI HPC - Nextflow Workflow
    ===================================================
    input_file_group: ${params.input_file_group}
    mets_path: ${params.mets_path}
    workspace_dir: ${params.workspace_dir}
    pages: ${params.pages}
    forks: ${params.forks}
    env_wrapper_cmd_step0: ${params.env_wrapper_cmd_step0}
    env_wrapper_cmd_step1: ${params.env_wrapper_cmd_step1}
    env_wrapper_cmd_step2: ${params.env_wrapper_cmd_step2}
    """.stripIndent()

process split_page_ranges {
    debug true
    maxForks params.forks

    input:
        val range_multiplier

    output:
        env mets_file_chunk
        env current_range_pages

    script:
        """
        current_range_pages=\$(ocrd workspace -d ${params.workspace_dir} list-page -f comma-separated -D ${params.forks} -C ${range_multiplier})
        echo "Current range is: \$current_range_pages"
        mets_file_chunk=\$(echo ${params.workspace_dir}/mets_${range_multiplier}.xml)
        echo "Mets file chunk path: \$mets_file_chunk"
        \$(cp -p ${params.mets_path} \$mets_file_chunk)
        """
}

process ocrd_dinglehopper_0 {
    debug true
    maxForks params.forks

    input:
        val mets_path
        val page_range
        val input_group
        val output_group

    output:
        val mets_path
        val page_range

    script:
        """
        ocrd-dinglehopper -w ${params.workspace_dir} -m ${mets_path} --page-id ${page_range} -I ${input_group} -O ${output_group}
        """
}

process ocrd_dinglehopper_1 {
    debug true
    maxForks params.forks

    input:
        val mets_path
        val page_range
        val input_group
        val output_group

    output:
        val mets_path
        val page_range

    script:
        """
        ocrd-dinglehopper -w ${params.workspace_dir} -m ${mets_path} --page-id ${page_range} -I ${input_group} -O ${output_group}
        """
}

process ocrd_dinglehopper_2 {
    debug true
    maxForks params.forks

    input:
        val mets_path
        val page_range
        val input_group
        val output_group

    output:
        val mets_path
        val page_range

    script:
        """
        ocrd-dinglehopper -w ${params.workspace_dir} -m ${mets_path} --page-id ${page_range} -I ${input_group} -O ${output_group}
        """
}

process merging_mets {
    debug true
    maxForks 1

    input:
        val mets_file_chunk
        val page_range

    script:
        """
        ocrd workspace -d ${params.workspace_dir} merge --force --no-copy-files ${mets_file_chunk} --page-id ${page_range}
        rm ${mets_file_chunk}
        """
}

workflow {
    main:
        ch_range_multipliers = Channel.of(0..params.forks.intValue()-1)
        split_page_ranges(ch_range_multipliers)
        ocrd_dinglehopper_0(split_page_ranges.out[0], split_page_ranges.out[1], params.input_file_group, "OCR-D-EVAL-SEG-BLOCK")
        ocrd_dinglehopper_1(ocrd_dinglehopper_0.out[0], ocrd_dinglehopper_0.out[1], "OCR-D-GT-SEG-LINE,OCR-D-OCR", "OCR-D-EVAL-SEG-LINE")
        ocrd_dinglehopper_2(ocrd_dinglehopper_1.out[0], ocrd_dinglehopper_1.out[1], "OCR-D-GT-SEG-PAGE,OCR-D-OCR", "OCR-D-EVAL-SEG-PAGE")
        merging_mets(ocrd_dinglehopper_2.out[0], ocrd_dinglehopper_2.out[1])
}
